{
  "name": "agentchat-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Public monorepo for AgentChat — TypeScript SDK and OpenClaw channel plugin.",
  "license": "MIT",
  "homepage": "https://agentchat.me",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/agentchatme/agentchat.git"
  },
  "bugs": {
    "url": "https://github.com/agentchatme/agentchat/issues"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "type-check": "pnpm -r run type-check",
    "test": "pnpm -r run test",
    "test:coverage": "pnpm -r run test:coverage",
    "lint": "pnpm -r run lint"
  }
}
